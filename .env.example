# ============================================================
# Platform Environment Variables
# Copy this to .env and fill in real values
# ============================================================

# -----------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------
# PostgreSQL connection string. For Neon DB, use your connection
# string from the Neon dashboard.
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# -----------------------------------------------------------
# AUTH / JWT (Required)
# -----------------------------------------------------------
# Secret for signing JWT access tokens. Must be at least 32 chars.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="generate-a-random-64-char-hex-string-here-minimum-32-chars"

# Access token expiry (default: 15 minutes)
JWT_EXPIRES_IN="15m"

# Refresh token expiry in days (default: 30)
REFRESH_TOKEN_EXPIRES_DAYS=30

# -----------------------------------------------------------
# REDIS (Optional — gracefully falls back to no-cache)
# -----------------------------------------------------------
# Redis connection URL for caching (notification counts, home page, rate limiting)
# If not provided, caching is skipped.
REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------
# S3 / FILE STORAGE (Optional — needed for file uploads)
# -----------------------------------------------------------
# S3-compatible storage for avatars, cover photos, attachments
S3_BUCKET="platform-uploads"
S3_REGION="us-east-1"
S3_ACCESS_KEY=""
S3_SECRET_KEY=""
# For S3-compatible services (MinIO, R2, etc.)
S3_ENDPOINT=""
# CDN URL prefix for serving uploaded files
CDN_URL=""

# -----------------------------------------------------------
# EMAIL (Optional — needed for email verification & notifications)
# -----------------------------------------------------------
# Sender address for outgoing emails
EMAIL_FROM="noreply@yourplatform.com"
# Use ONE of these email providers:
SENDGRID_API_KEY=""
RESEND_API_KEY=""

# -----------------------------------------------------------
# BUILDER HUB INTEGRATION (Required for Builder Hub auth)
# -----------------------------------------------------------
# Ava Labs Builder Hub base URL
BUILDER_HUB_URL="https://build.avax.network"
# Endpoint for validating JWT tokens from Builder Hub
BUILDER_HUB_VALIDATE_URL="https://build.avax.network/api/validate-jwt-token"
# Your registered app credentials with Builder Hub
BUILDER_HUB_CLIENT_ID=""
BUILDER_HUB_CLIENT_SECRET=""
# PUBLIC URL for the Builder Hub login popup (exposed to browser)
NEXT_PUBLIC_BUILDER_HUB_LOGIN_URL="https://build.avax.network"

# -----------------------------------------------------------
# APP CONFIG (Required)
# -----------------------------------------------------------
# Public-facing URL of your app (used for redirects, CORS, etc.)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Environment: development | production | test
NODE_ENV="development"
